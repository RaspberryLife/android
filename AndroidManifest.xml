<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.hsrw.heimautomatisierung"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.INTERNET" >
	</uses-permission>

	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" >
	</uses-permission>
    
    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="org.hsrw.heimautomatisierung.activities.SplashScreen"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen" 
            android:noHistory="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name="org.hsrw.heimautomatisierung.activities.SelectActivity" android:label="@string/app_name" >
        </activity>
        <activity android:name="org.hsrw.heimautomatisierung.activities.Monitoring" android:label="@string/app_name" >
        </activity>
        <activity android:name="org.hsrw.heimautomatisierung.activities.ProtobufTest" android:label="@string/app_name" >
        </activity>
        <activity android:name="org.hsrw.heimautomatisierung.activities.AlarmClock" android:label="@string/app_name" >
        </activity>
    </application>

</manifest>
